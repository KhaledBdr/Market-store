<%- include('parts/header') %> 
<body>
    <%- include('parts/nav-bar') %> 
<div class="container mt-5">
    <table class="table table-primary table-bordered text-center p-0 m-0">
        <thead class="table-dark">
            <td class="me-5">no</td>
            <td class="me-5">Product Name</td>
            <td class="me-5">Amount</td>
            <td class="me-5">Cost</td>
            <td class="me-5">Address</td>
            <td class="me-5">Status</td>
            <td class="me-5">Time</td>
            <td class="d-block btn btn-danger fw-bolder bg-danger" >Cancle</td>
        </thead>
            <% if(items.length !== 0){ %>
                <% for (let i = 0; i < items.length; i++) { %>
             <tr>
                    <td class="bg-primary text-light fw-bolder"> <%=i+1  %> </td>
                    <td> <%= items[i].name  %> </td>
                    <td> <%= items[i].amount  %> </td>
                    <td> <%= items[i].price  %> </td>
                    <td> <%= items[i].Address  %> </td>
                    <td> <%= items[i].state  %> </td>
                    <td> <%= items[i].timestamp  %> </td>
                    <td>
                    <form action="/order" method="post">
                    <input type="hidden" name="order_Id" value="<%= items[i]._id %>">
                    <input type="submit" class=" btn btn-danger w-100" formaction="/orders/cancle" value="Cancle">
                    </form>
                </td>
            </tr>
                <% } %> 

            <% } %> 
        </table>
    </div>
        

    <%- include('parts/footer') %> 
</body>